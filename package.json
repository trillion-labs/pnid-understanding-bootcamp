{
  "name": "pnid-understanding-bootcamp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "site:prepare": "node scripts/prepare-site-content.mjs",
    "dev": "npm run site:prepare && astro dev",
    "build": "npm run site:prepare && astro build",
    "preview": "astro preview",
    "check": "npm run site:prepare && astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.0",
    "astro": "^7.2.10",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "typescript": "^5.9.2"
  }
}
